(function () {
  'use strict';
  var WFApp = (window.WFApp = window.WFApp || {});
  WFApp.pages = WFApp.pages || {};

  WFApp.pages.PrivacyPolicy = {
    init: function ({ container }) {
      return { destroy: function () {} };
    }
  };
})();
